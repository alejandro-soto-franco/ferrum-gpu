[package]
name = "ferrum-gpu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure-Rust GPU compute substrate. Facade crate re-exporting the core trait + ergonomic Device/Buffer wrappers."

[features]
default = ["cuda"]
cuda = ["dep:ferrum-gpu-cuda", "dep:cudarc"]

[dependencies]
bytemuck = { workspace = true }
cudarc = { workspace = true, optional = true }
ferrum-gpu-core = { workspace = true }
ferrum-gpu-cuda = { workspace = true, optional = true }
parking_lot = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
